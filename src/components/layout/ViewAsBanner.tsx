// Role previewing is intentionally disabled. Every portal is rendered from
// the authenticated profile returned by Supabase, not client-side state.
export default function ViewAsBanner() {
  return null
}
